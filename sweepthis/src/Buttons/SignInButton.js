import React from 'react';

const signInButton = () => {
    return (
        <button className="SignInButton" type="button">
            Sign In
        </button>
    )
};

export default signInButton;